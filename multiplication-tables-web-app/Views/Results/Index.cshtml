
@model Test

<h1>Results</h1>

<table class="table">
    @for (int i = 0; i < Model.questions.Count; i++)
    {
        <tr>
            <td>
                @Model.questions[i].num1 x @Model.questions[i].num2 =
            </td>
            <td>
                @Model.questions[i].answer
            </td>
        </tr>
    }
</table>